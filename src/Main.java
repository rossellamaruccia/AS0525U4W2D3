import Entities.Order;
import Entities.Product;

void main() {


    List<Product> priceFilter = Arrays.stream().filter(product -> product.price < 100 && product.category = "Book").toList();
    List<Order> categoryFilter = Arrays.stream().filter(Order.product _> product.category = "Baby").toList();
    
}
